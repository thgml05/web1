class BasicCar {
    constructor(color, maxSpeed) {
        this.color = color;
        this.maxSpeed = maxSpeed;
        this.price = price;
    }

    run() {
        console.log("Running...");
    }
}